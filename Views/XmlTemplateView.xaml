<UserControl x:Class="WpfXmlDemo.XmlTemplateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:WpfXmlDemo"
        Height="400" Width="600" >
    
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DockPanel LastChildFill="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <StackPanel>
                    <StackPanel x:Name="stkItems" x:FieldModifier="public" Orientation="Vertical">
                    </StackPanel>
                    <Button Name="SaveButton" Content="Save" VerticalAlignment="Bottom" Margin="10,10,10,10" Height="50" FontSize="28"/>
                </StackPanel>
            </DockPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
